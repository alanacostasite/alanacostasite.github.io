{
  "name": "curtmenusite",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev -p 3066",
    "test": "npm run build && npm run export",
    "build": "next build",
    "start": "next start",
    "export": "next export",
    "deploy": "git stash && next build && next export && deploy.bat"
  },
  "dependencies": {
    "@fortawesome/fontawesome-svg-core": "^6.1.1",
    "@fortawesome/free-brands-svg-icons": "^6.1.1",
    "@fortawesome/free-solid-svg-icons": "^6.1.1",
    "@fortawesome/react-fontawesome": "^0.1.18",
    "next": "^12.1.0",
    "react": "17.0.2",
    "react-dom": "17.0.2"
  }
}
